{
  "name": "react-monorepo",
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.7"
  },
  "scripts": {
    "start:customhook": "lerna run start --scope customhook",
    "start:useeffect": "lerna run start --scope useeffect",
    "build": "lerna run build",
    "dev": "lerna run dev",
    "use-node22": "source ~/.nvm/nvm.sh && nvm use 22"
  }
}
